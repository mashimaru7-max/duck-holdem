import {describe,expect,it} from 'vitest';import {compareScore,createDeck,evaluate} from './cards.js';import {applyAction,continueAfterHand,newPlayer,newRoom,startHand} from './engine.js';
describe('cards',()=>{it('52장 덱이 모두 유일하다',()=>{const d=createDeck();expect(d).toHaveLength(52);expect(new Set(d).size).toBe(52);});it('족보와 A2345를 판정한다',()=>{expect(evaluate(['AS','KS','QS','JS','TS']).name).toBe('스트레이트 플러시');expect(evaluate(['AS','2D','3H','4C','5S']).values[0]).toBe(5);expect(compareScore(evaluate(['AS','AD','KH','QC','2S']),evaluate(['KS','KD','QH','JC','2D']))).toBeGreaterThan(0);});});
describe('engine',()=>{it('헤즈업 블라인드와 중복 명령을 처리한다',()=>{const a=newPlayer('A','a',1),b=newPlayer('B','b',2),r=newRoom(a,'1234');r.players.push(b);a.ready=b.ready=true;startHand(r,()=>0.5);expect(r.sbSeat).toBe(r.dealerSeat);const actor=r.players.find(p=>p.seat===r.actionSeat)!;const v=r.version;applyAction(r,actor.id,'call','same',v);const after=r.version;applyAction(r,actor.id,'call','same',v);expect(r.version).toBe(after);});it('전원 폴드 시 결과를 기록하고 새 게임으로 전환한다',()=>{const a=newPlayer('A','a',1),b=newPlayer('B','b',2),r=newRoom(a,'1234');r.players.push(b);a.ready=b.ready=true;startHand(r,()=>0.2);const actor=r.players.find(p=>p.seat===r.actionSeat)!;applyAction(r,actor.id,'fold','f',r.version);expect(r.status).toBe('HAND_END');expect(r.result?.winners).toHaveLength(1);expect(r.players.reduce((s,p)=>s+p.stack,0)).toBe(200);continueAfterHand(r,true);expect(r.status).toBe('WAITING');expect(r.players.every(p=>p.stack===100&&!p.ready)).toBe(true);});});
